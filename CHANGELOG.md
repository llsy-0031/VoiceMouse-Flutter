# Changelog

本项目版本号遵循 Semantic Versioning（MAJOR.MINOR.PATCH）。

## [1.0.3] - 2026-08-21

### Added
- 新增系统托盘支持：点击最小化/关闭按钮时隐藏到系统托盘，双击托盘图标或选择「显示主界面」可恢复窗口。
- 新增自定义应用图标：任务栏、系统托盘与窗口标题栏均使用 VoiceMouse 专属鼠标+麦克风图标（16x16~256x256 多分辨率 ICO）。
- 托盘右键菜单支持：显示主界面、暂停/继续运行、退出程序。
- 首次最小化到托盘时，托盘 Tooltip 临时提示「已最小化到托盘，右键图标可退出」。

## [1.0.2] - 2026-08-21

### Added
- 录制校准完成后自动发送一次快捷键做「验货」，无需再手动点测试。
- 「完全替换」模式增加红色警告条与警示图标，防止用户误切导致原按键功能失效。
- 关于页顶部大字号琥珀色提示紧急停用快捷键 `Ctrl+Alt+F12`。
- 录制中灰显区域追加琥珀色小字提示，说明需先点「录制完成」再操作。

## [1.0.1] - 2026-08-20

### Fixed
- macOS：修改任意设置后语音触发永久失效（设置页把显示文本"FN 连按两下"写入快捷键，触发时无法解析）。
- macOS：语音触发后下一次鼠标点击被穿透（键盘注入误设放行标记）。
- macOS：双击保留原功能时抬起事件被吞，导致按键像被按住不放。
- macOS：快捷键录制会把先按下的修饰键本身录成主键（如 Ctrl 录成 CTRL+Control）。
- macOS：窗口为横屏 1280x720 / 800x600，改为竖屏 460x800，可完整展示全部内容。
- 设置页/运行页副标题文案与实际交互语义不一致（单击触发 · 双击保留）。

### Added
- macOS：紧急热键 Ctrl+Alt+F12（监听系统按键，不吞事件），与 Windows 行为一致。

## [1.0.0] - 2026-08-20

### Added
- Windows x64 + macOS（Apple Silicon）双平台发布。
- 鼠标按键触发系统语音输入：单击触发 / 双击保留原功能 / 长按兜底 / 替换模式。
- 自动识别触发按键、快捷键录制校准、测试快捷键。
- 安全保护：文本输入场景判断（Windows 前台窗口检测）、紧急停用热键。
- 开机自启、深浅色主题、使用统计。
- macOS 版经 GitHub Actions 云构建（ad-hoc 签名，未公证）。

### Known Issues（1.0.1）
- macOS 无全屏/游戏窗口自动识别，全屏场景请用 Ctrl+Alt+F12 紧急停用。
- macOS 设备列表暂为空（无设备枚举）。
- 快捷键录制期间，"测试快捷键"注入会被拦截（录制态边缘行为）。
- 保留原功能补发时，若点击后立即移动鼠标，光标会被拉回点击位置。
- 构建为 ad-hoc 签名，Gatekeeper 首次打开需右键 -> 打开。